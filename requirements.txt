# RUNTIME dependencies — must stay in sync with [project.dependencies] in
# pyproject.toml, which is what Vercel actually installs (it resolves with
# `uv lock`, and once a pyproject.toml exists this file is no longer read).
# Kept for local installs and for anyone not using uv.
#
# For local development, training, or the diffusion engine:
#
#     pip install -r requirements-dev.txt
#
# Deliberately torch-free AND scipy-free. Measured Linux cp312 wheels,
# uncompressed — the deployment limit is 250 MB:
#
#     this set          149 MB
#     + scipy           264 MB   over the limit
#     + torch stack      ~4 GB   never a possibility
#
# scipy is avoidable but not by accident: trimesh reaches for it from
# Trimesh.copy() and from vertex_normals. builder._finish routes around both.
# tests/test_api.py fails if either creeps back.

# ── Detection ───────────────────────────────────────────────────────
onnxruntime==1.28.0       # CPU build. NOT onnxruntime-gpu (298 MB, needs CUDA)
pillow==12.2.0

# ── Geometry ────────────────────────────────────────────────────────
numpy==2.2.6
shapely==2.1.2
trimesh==4.11.5
networkx==3.4.2           # trimesh graph ops
mapbox-earcut==2.0.0      # polygon triangulation
manifold3d==3.4.1         # boolean backend for the door/window openings
