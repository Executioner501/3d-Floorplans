# RUNTIME dependencies — this is what Vercel installs for api/generate.py.
#
# Keep this file minimal. It is deliberately torch-free: ultralytics + torch
# is ~4 GB installed and cannot fit in a serverless bundle, so detection runs
# through ONNX Runtime instead (see detect_onnx.py). Measured Linux cp312
# wheels total ~48 MB compressed, well inside Vercel's 250 MB unzipped limit.
#
# For local development, training, or the diffusion engine, install the full
# set instead:
#
#     pip install -r requirements-dev.txt

# ── Detection ───────────────────────────────────────────────────────
onnxruntime==1.28.0       # CPU build. NOT onnxruntime-gpu (298 MB, needs CUDA)
pillow==12.2.0

# ── Geometry ────────────────────────────────────────────────────────
numpy==2.2.6
shapely==2.1.2
trimesh==4.11.5
networkx==3.4.2           # trimesh graph ops
mapbox-earcut==2.0.0      # polygon triangulation
manifold3d==3.4.1         # boolean backend for the door/window openings
