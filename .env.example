# Copy to `.env` and fill in. `.env` is gitignored — never commit real keys.
#
# Only needed for the optional LLM roof designer (main.py ENGINE = "llm").
# The default RAG engine runs fully offline and needs no keys at all.

GEMINI_API_KEY=
